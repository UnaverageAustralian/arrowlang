import io

$main ( -> u8 )
    0 while dup 10 < loop
        dup io::println
        1+
    end drop

    0 loop
        dup 10 >= if break end
        dup io::println
        1+
    end

    io::flush
    0
$
