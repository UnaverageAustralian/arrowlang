import std::io

$main ( -> u8 )
    0 while dup 10 < loop
        dup std::io::println
        1+
    end drop

    0 loop
        dup 10 >= if break end
        dup std::io::println
        1+
    end
    0
$
